# Logo Variants

Cleanup complete. Older explorations were removed after selecting the final direction.

Current files:

- `logo.svg` — current chosen final SVG mark
- `logo.png` — raster export / preview companion
